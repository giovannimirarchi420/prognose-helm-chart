apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-fe-service
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "resource-management-chart.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ include "resource-management-chart.name" . }}
    component: frontend